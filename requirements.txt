# Core - used by src/data.py, needed by every milestone that imports it
numpy
scikit-learn # sklearn

# EDA / notebooks only - not imported by src/
pandas       # tables (describe, value_counts) in notebooks/00_project_summary.ipynb, 01_eda.ipynb, 02_feature_engineering.ipynb, 03_baseline.ipynb, 04_bev_cnn.ipynb
matplotlib   # plots in notebooks/00_project_summary.ipynb, 01_eda.ipynb, 02_feature_engineering.ipynb, 03_baseline.ipynb, 04_bev_cnn.ipynb
ipykernel    # run notebooks/ in Jupyter

# M3 - CNN on BEV grids (src/cnn.py, notebooks/04_bev_cnn.ipynb)
# On Windows, `pip install torch` can fail with WinError 206 (path too long):
# the wheel's bundled third-party license tree nests deeper than 260 chars.
# Fix is enabling Win32 long paths (Windows Settings, or as admin:
# `New-ItemProperty -Path "HKLM:\SYSTEM\CurrentControlSet\Control\FileSystem"
# -Name LongPathsEnabled -Value 1 -PropertyType DWORD -Force`), then a normal
# install works.
torch
